import { MediaCategoryPipe } from './media-category.pipe';

describe('MediaCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
