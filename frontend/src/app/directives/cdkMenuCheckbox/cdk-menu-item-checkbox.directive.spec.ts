import { MenuItemCheckboxDirective } from './cdk-menu-item-checkbox.directive';

describe('CdkMenuItemCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuItemCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
