import { UserScrollDirective } from './user-scroll.directive';

describe('UserScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new UserScrollDirective();
    expect(directive).toBeTruthy();
  });
});
