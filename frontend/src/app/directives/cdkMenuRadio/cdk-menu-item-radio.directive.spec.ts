import { MenuItemRadioDirective } from './cdk-menu-item-radio.directive';

describe('CdkMenuItemRadioDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuItemRadioDirective();
    expect(directive).toBeTruthy();
  });
});
